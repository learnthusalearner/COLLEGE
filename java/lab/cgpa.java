import java.util.Scanner;
class box
{
	double roll;
	String name;
	float cgpa;
}
class cgpa{

	static void student(double roll,String name,float cgpa)
	{
		System.out.println("roll number is"+roll);
		System.out.println("roll number is"+roll);
		System.out.println("roll number is"+roll);
	}
	public static void main(String args[])
	{
	   Scanner scan=new Scanner(System.in);
		box stud=new box();
		stud.roll=scan.nextDouble();
		stud.name=scan.nextString();
		stud.cgpa=scan.nextfloat();
		area(box1.length,box1.breadth);
	}
}